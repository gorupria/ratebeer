<p id="notice"><%= notice %></p>

<h1>Breweries</h1>
<p>Number of breweries: <%= @breweries.count %></p>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Year</th>
      <th>No. of beers</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @breweries.each do |brewery| %>
      <tr>
        <td><h2><%= link_to brewery.name, brewery %></h2></td>
        <td><em>Established in <%= brewery.year %></em></td>
        <td><%= brewery.beers.count %></td>
        <td><%= link_to 'Edit', edit_brewery_path(brewery) %></td>
        <td><%= link_to 'Destroy', brewery, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Brewery', new_brewery_path %>
<br>
<%= link_to 'List of all beers', beers_path %>
